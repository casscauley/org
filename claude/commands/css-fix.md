Look at all css and style tags in this repo and ~/projects/unrest-ui

- replace margins with flexbox (or grid) and gap any where multiple elements are evenly spaced. Use margin bottom sparinly ask me to justify any margin-bottom uses

- replace all inline styles with css classes, prefering to use the component classes of unrest-ui where possible, but use tailwind utility classes as necessary

- replace all hard-coded colors with css variables

- if there's any place you keep a hard coded color, margin, or style attribute, let me know