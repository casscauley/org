Review this conversation and identify any code conventions, style preferences, or workflow patterns that should be documented in ~/.claude/CLAUDE.md.

For each convention you find:
1. Check if it's already in ~/.claude/CLAUDE.md
2. If it's new or an update to an existing rule, include it in your proposal

Present the proposed changes as a diff before editing. Wait for my approval before modifying the file.
